spring:
  cache:
    type: none
  data:
    redis:
      repositories:
        enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        validation:
          mode: none

  flyway:
    enabled: false

service:
  security:
    internal-secret: "Internal_Secret_Vpn_App_2024_!@#"
    admins:
      - 999999